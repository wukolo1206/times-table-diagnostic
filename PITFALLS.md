# PITFALLS — 九九乘法熟練度診斷

## 已知風險

- **熟練門檻 3000 ms 未經本地實證**，需用第一次課堂診斷的實際分布校準
- ~~**GAS 實際併發上限未確認**~~ → 2026-08-29 實測：33 筆同時上傳全數成功，
  最慢 26.9 秒。`LockService` 把寫入序列化，時間隨人數線性增加，
  35 人加上座號錯開後應在 30 秒內完成。**人數再往上（兩班以上同時）需重測**
- **去重掃描窗固定 5000 列**，涵蓋約 7 班 × 7 天。啟用班級超過 7 班時必須調高
  （`Code.gs` 的 `DEDUP_SCAN_ROWS`），否則舊場次的重複送出攔不到
- **CORS 尚未在實機平板驗證**：桌機 Chrome 測不出來，上課前必須用真平板＋校內網路試過
  （桌機以 curl 與 Playwright 測 POST 均正常，但那不算數）

## 坑

### 測試指令用 `| tail` 接 `&&` 會吃掉失敗（2026-08-29）

`node test.js | tail -4 && git commit` —— 管線的離開碼是 `tail` 的，永遠是 0，
所以測試失敗也照樣 commit。已因此提交過一次紅燈狀態。
改用 `node test.js > out.txt 2>&1; echo "exit=$?"` 再看檔案。

### 佇列測試用小數字當時間戳會被 TTL 吃掉（2026-08-29）

待傳佇列有 7 天 TTL。測試裡寫 `q.push(p, 1000)` 等於 1970 年，
`list()` 用 `Date.now()` 比對就全被判定過期清除，症狀是「存進去卻讀不到」。
測試的時間戳必須以 `Date.now()` 為基準。

### 手速校準按下後圓圈沒立刻變灰（2026-08-29）

按下後仍維持綠色 0.5 秒，使用者（與自動測試）會把同一次誤認為下一次而重複按，
第二次按下無效，五次永遠湊不滿。修法是 `onHit` 立刻把 `.go` 拿掉。

### 檢查「畫面不該出現某字」要看可見文字，不能看原始碼（2026-08-29）

端到端測試用 `page.content()` 檢查 me.html 不出現「不會」，結果抓到程式註解裡的
「不說『不會』」而誤判失敗。改用 `page.inner_text('body')`。

### 查詢參數不可命名為 `c`（2026-08-29，花了最久的一個坑）

GAS Web App **保留 `c` 這個查詢參數名稱**。任何帶 `?c=` 的請求會在進入 `doGet` 之前
就被 Google 擋掉，回 HTTP 400 加上一頁 Google 雲端硬碟的
「很抱歉，目前無法開啟這個檔案」——看起來完全像是部署失敗或權限沒設好。

實測：

| 網址 | 結果 |
|---|---|
| `?x=1` | 正常 |
| `?a=1&b=2` | 正常 |
| `?action=config` | 正常 |
| **`?c=DEMO01`** | **HTTP 400** |
| `?cls=DEMO01` | 正常 |

本專案班級代碼參數因此命名為 `cls`。`code` / `klass` / `cc` 實測也可用。

**為什麼這個坑特別貴：** 錯誤訊息把人引導到完全錯誤的方向。當時排查順序是
權限 → 專案類型（綁定／獨立）→ manifest → 建立方式（API／編輯器）→ 網路 → 帳號，
每一項都要使用者手動操作重試，全部無效。真正的線索是使用者截圖顯示
「開不帶參數的網址是正常的」——**不帶參數會通、帶參數不通**，才把方向轉到參數本身。

**教訓：** 遇到 GAS Web App 回這個錯誤頁時，先用 `?x=1` 試最單純的參數。
一分鐘就能區分「部署壞了」和「參數名撞到保留字」。

**另一個同時發現的錯誤：** 排查中我拿 `past-exam-trainer` 的 `appsscript.json`
當「正確範本」，據此刪掉 `webapp` 區塊——但那個專案的網址其實是 404（已失效），
根本不能當對照組。後來比對 5 個**確認活著**的專案，它們全都有 `webapp` 區塊。
**挑對照組之前要先確認對照組本身是活的。**

### 小數毫秒讓整份上傳被退回（2026-08-29）

`performance.now()` 回傳小數（如 2140.4），但資料契約要求 `Number.isInteger(ms)`，
所以學生做完 81 題、畫面顯示「完成」，伺服器卻以 `BAD_DETAIL` 整份退回，
試算表一筆都沒有。**學生和老師都不會知道資料掉了。**

兩層測試剛好都繞過這個地方：

- Node 核心測試餵的時間戳都是整數，永遠測不到
- 端到端測試的假伺服器直接回 `{ok:true}`，等於沒做資料檢查

修法：`evaluateTiming` 內 `Math.round`。並補兩道防線——核心測試驗證小數輸入產生整數；
**端到端的假伺服器改為比照資料契約檢查 POST 內容**，不再無條件回 ok。

**教訓：假伺服器如果不驗證，端到端測試就只證明了「前端有送出東西」，
不證明「送出的東西伺服器收得下」。** 契約檢查要放在假伺服器裡。

### 未完成的場次原本不會上傳（2026-08-29）

設計文件 8.1 寫了「中途離開以 partial 上傳」，但實作只在做完全部題目時呼叫
`finishSession`，其餘路徑一概沒有。更糟的是續作詢問按「取消」時直接
`clearProgress()`，把真實作答紀錄丟掉。

修法：抽出 `buildPayload` / `upload` / `uploadAbandoned`，
作答畫面加「先做到這裡」，續作取消與共用平板的他人殘留都先送出再清除。

**教訓：規格寫了但沒實作的功能，測試也不會提醒你——因為測試是照著實作寫的。
自我檢查時要拿規格逐條對，不是拿實作逐條對。**

### 靜態檔快取讓裝置繼續跑舊版（2026-08-29）

GitHub Pages 與瀏覽器都會快取 `.js` / `.css`。推上新版後，裝置可能還在跑舊的，
**而且完全沒有錯誤訊息**——症狀是「新功能怎麼不見了」，或更難查的
「行為跟預期不一樣」。這在教室裡特別麻煩：修好的問題，學生平板上還在發生。

實際踩到兩次：

1. 自動化測試抓到舊的 `fact-core.js`，導致剛改好的範圍參數看起來沒生效，
   一度以為程式有 bug
2. 使用者手機看不到新加的「精熟練習」選項，線上檔案其實是對的

**解法：`python tools/stamp_assets.py`**，把 `src="fact-core.js"` 改寫成
`src="fact-core.js?v=<時間戳>"`。改完前端、commit 之前一定要跑。

**臨時處理**：手機長按重新整理選「重新載入」，或到瀏覽器設定清除該站資料。

### 教師 Dashboard 只看得到「做過測驗的人」（2026-08-29）

`apiDashboard` 原本直接把 `熟練度快照` 當成全班名單，但快照**只有做過測驗的人才有列**。
32 人的班只有 1 人做過時，Dashboard 回傳的 students 只有 1 筆。

三個後果，第三個最嚴重：

1. 老師看不到誰還沒做（課堂上正是要點這個）
2. 涵蓋率的分母錯了
3. **「有效樣本不足就標斜紋」的保護完全失效**——系統以為全班只有 1 人，
   1 人做完就是 100%，於是拿一個人的資料告訴老師「明天要補這兩題」。
   設計文件 8.2 特地設了 60% 門檻防止這件事，卻因為分母錯了而沒有生效

**修法：一律以 `學生名單` 為準**，沒有快照的人補上 81 格空白（`FactCore.emptyCells()`）。

**教訓：衍生資料表不可以拿來當名單。** 快照是「有紀錄的人」，名單才是「全班」，
兩者混用時，所有以「全班人數」為分母的統計都會靜靜地算錯。

### 刪資料一定要連快照一起重算（2026-08-30）

`熟練度快照` 是由場次算出來的衍生資料。只刪 `作答場次` 的列而不重算快照，
熱圖會繼續顯示已刪資料算出來的顏色——**資料刪了但畫面沒變**，
老師會以為刪除失敗，或更糟：以為那些顏色是真的。

`apiDeleteSession` / `apiDeleteGroup` 刪完都會呼叫 `updateSnapshot_`。

### 端到端測試的 dialog handler 是全域的（2026-08-30）

`pg.on('dialog', ...)` 註冊後對整個 page 生效，會影響後面所有測試。
診斷流程裝了「一律確定」，導致教師頁「按取消不會刪」永遠測不到——
取消被前面那個 handler 搶先確定了。

改用具名函式並在需要處 `pg.remove_listener('dialog', auto_accept)`。
