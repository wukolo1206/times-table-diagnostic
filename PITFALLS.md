# PITFALLS — 九九乘法熟練度診斷

## 已知風險

- **熟練門檻 3000 ms 未經本地實證**，需用第一次課堂診斷的實際分布校準
- ~~**GAS 實際併發上限未確認**~~ → 2026-08-29 實測：33 筆同時上傳全數成功，
  最慢 26.9 秒。`LockService` 把寫入序列化，時間隨人數線性增加，
  35 人加上座號錯開後應在 30 秒內完成。**人數再往上（兩班以上同時）需重測**
- **去重掃描窗固定 5000 列**，涵蓋約 7 班 × 7 天。啟用班級超過 7 班時必須調高
  （`Code.gs` 的 `DEDUP_SCAN_ROWS`），否則舊場次的重複送出攔不到
- **CORS 尚未在實機平板驗證**：桌機 Chrome 測不出來，上課前必須用真平板＋校內網路試過
  （桌機以 curl 與 Playwright 測 POST 均正常，但那不算數）

## 坑

### 測試指令用 `| tail` 接 `&&` 會吃掉失敗（2026-08-29）

`node test.js | tail -4 && git commit` —— 管線的離開碼是 `tail` 的，永遠是 0，
所以測試失敗也照樣 commit。已因此提交過一次紅燈狀態。
改用 `node test.js > out.txt 2>&1; echo "exit=$?"` 再看檔案。

### 佇列測試用小數字當時間戳會被 TTL 吃掉（2026-08-29）

待傳佇列有 7 天 TTL。測試裡寫 `q.push(p, 1000)` 等於 1970 年，
`list()` 用 `Date.now()` 比對就全被判定過期清除，症狀是「存進去卻讀不到」。
測試的時間戳必須以 `Date.now()` 為基準。

### 手速校準按下後圓圈沒立刻變灰（2026-08-29）

按下後仍維持綠色 0.5 秒，使用者（與自動測試）會把同一次誤認為下一次而重複按，
第二次按下無效，五次永遠湊不滿。修法是 `onHit` 立刻把 `.go` 拿掉。

### 檢查「畫面不該出現某字」要看可見文字，不能看原始碼（2026-08-29）

端到端測試用 `page.content()` 檢查 me.html 不出現「不會」，結果抓到程式註解裡的
「不說『不會』」而誤判失敗。改用 `page.inner_text('body')`。

### 查詢參數不可命名為 `c`（2026-08-29，花了最久的一個坑）

GAS Web App **保留 `c` 這個查詢參數名稱**。任何帶 `?c=` 的請求會在進入 `doGet` 之前
就被 Google 擋掉，回 HTTP 400 加上一頁 Google 雲端硬碟的
「很抱歉，目前無法開啟這個檔案」——看起來完全像是部署失敗或權限沒設好。

實測：

| 網址 | 結果 |
|---|---|
| `?x=1` | 正常 |
| `?a=1&b=2` | 正常 |
| `?action=config` | 正常 |
| **`?c=DEMO01`** | **HTTP 400** |
| `?cls=DEMO01` | 正常 |

本專案班級代碼參數因此命名為 `cls`。`code` / `klass` / `cc` 實測也可用。

**為什麼這個坑特別貴：** 錯誤訊息把人引導到完全錯誤的方向。當時排查順序是
權限 → 專案類型（綁定／獨立）→ manifest → 建立方式（API／編輯器）→ 網路 → 帳號，
每一項都要使用者手動操作重試，全部無效。真正的線索是使用者截圖顯示
「開不帶參數的網址是正常的」——**不帶參數會通、帶參數不通**，才把方向轉到參數本身。

**教訓：** 遇到 GAS Web App 回這個錯誤頁時，先用 `?x=1` 試最單純的參數。
一分鐘就能區分「部署壞了」和「參數名撞到保留字」。

**另一個同時發現的錯誤：** 排查中我拿 `past-exam-trainer` 的 `appsscript.json`
當「正確範本」，據此刪掉 `webapp` 區塊——但那個專案的網址其實是 404（已失效），
根本不能當對照組。後來比對 5 個**確認活著**的專案，它們全都有 `webapp` 區塊。
**挑對照組之前要先確認對照組本身是活的。**
