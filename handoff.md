# 工作交接 — 2026-08-31

## 已完成

**從零到上線，並經真機實測與十二輪改進（v0.1.0 → v0.12.0）**

| 版本 | 重點 |
|---|---|
| v0.1.0 | Phase 1：四頁 ＋ GAS 後端 ＋ 四張表，GitHub Pages 上線 |
| v0.2.0 | 修上傳被退回、未完成場次也上傳、自選測驗範圍 |
| v0.3.0 | 錯誤型態分析、單一學生檢視、家長通知訊息 |
| v0.4.0 | 精熟練習 `sprint.html`，與診斷分開 |
| v0.5.x | 九枚徽章、個人最佳、進度條；修 Dashboard 誤把快照當全班 |
| v0.6.x | 一位數自動送出、個人頁加練習入口；班級代碼改好打 |
| v0.7.0 | 每一次測驗的逐題明細、學生頁加老師入口 |
| v0.8.0 | 全班單場報表（誰做了／每題統計／常犯的錯） |
| v0.8.1 | 修時間差 8 小時（存 UTC 卻直接顯示，連分組都用 UTC 日期）、全班報表卡在載入中 |
| v0.8.2 | 勾選刪除（單場／勾選數人／全選＝整場）、B 表可點欄位排序 |
| v0.9.0 | 精熟練習預設三分鐘、可選 1／2／3／5 分；不同長度的成績分開比 |
| v0.10.0 | 練習成績比較圖表（內嵌 SVG，不引外部套件） |
| v0.11.0 | 教師頁改**分頁**：四個上層頁籤＋單場報表內 A／B／C 小頁籤 |
| v0.12.0 | 熱圖加**進步視圖**（第一次 vs 最近一次）＋頑固格清單＋格內歷次秒數 |

**本次 session（2026-08-31）做的兩件事**

1. **教師頁改分頁**（v0.11.0）——原本一路往下捲太長
   - 上層四頁籤：熱圖總覽／單場報表／看單一學生／設定與刪除
   - 單場報表內再分 A 誰做了／B 每題表現／C 常犯的錯
   - 採「搬移既有 DOM 節點」而非重產 HTML，已綁定的事件與已載入的資料都跟著走

2. **進步視圖**（v0.12.0）——回答「練了到底有沒有用」
   - 工具列新增「看法」：現在的熟練狀況／練了之後的進步
   - 進步視圖比較每人第一次 vs 最近一次：
     🟩 重練過的人都熟練了／🟨 有人變好還有人沒到／
     🟥 有人練 3 次以上仍未熟練／⬜ 沒人重複練過（無從比較）
   - 新增「練了還是卡住的算式」清單，指名是哪幾位學生
   - 點格子加列歷次秒數（8.2 → 5.1 → 4.8 秒），
     分得出「有在進步只是還沒過門檻」與「完全沒動」
   - 後端新增 `progress` 端點，只回原始毫秒，
     等級與顏色一律前端依當下門檻計算（延續 D3，見 DECISIONS D20）

**真實班級：碧小408**

- 代碼 `4088`、教師 PIN `1257`、座號 1–32、純座號模式（不存姓名）、練習預設 3 分鐘
- 示範班 `DEMO01`（PIN `1234`）保留供測試

**已收錄進「Vibe Coding 資源庫」**（學科／數學（跨年段），排序 1330）

**驗收**：核心 253 ＋ 上傳層 54 ＋ 端到端 58 全過；
GAS 已重新部署（網址不變），線上以真實網址驗證 `progress` 端點正常
（碧小408 回 32 人、981 筆格資料）。

## 目前進度

功能完整、線上可用、手機實測過。**碧小408 隨時可進教室。**

## 未完成／待確認

- **真平板 ＋ 校內網路驗證**（唯一硬性缺口；手機 5G 已過，剩學校防火牆／proxy）
- **碧小408 目前那 981 筆是測試資料，正式用之前要先清掉**
- 熟練門檻 3000 ms 待用真實班級資料校準
- 「卡住」的判定門檻定在練 3 次（`STUBBORN_TRIES`），待真實資料檢驗會不會太寬或太嚴
- 精熟練習的答錯懲罰採建議方案（不扣分＋鎖 1.5 秒），使用者原提過扣分，改動只需一行
- Phase 2 剩餘：三榜排行、全班同步衝刺、`admin.html`、
  設計文件 7.2 的遺忘因子（需快照存每格上次作答日期）

## 下一步

1. **用勾選刪除清掉碧小408 的測試場次**（教師頁 →「單場報表」頁籤 → 選一場 → 全選 → 刪掉整場），
   讓熱圖回到乾淨狀態再正式使用
2. 拿真平板到學校連校內網路做一次完整作答（9 題或 60 秒練習即可）
3. 碧小408 全班跑一次診斷 → 全班報表就有資料
4. 讓學生練幾次之後切到「練了之後的進步」，檢查頑固格清單是否符合課堂觀察
5. 用實際反應時間分布校準熟練門檻
6. 依 `DECISIONS.md` Phase 2 章節做剩餘功能

## 注意事項

**改前端後一定要跑 `python tools/stamp_assets.py`**，否則裝置繼續跑舊版且無錯誤訊息。

**新加的卡片一定要登記進 `TABS`**：`buildTabs()` 會把沒登記的卡片連同 `oldCols` 一起刪掉，
`$('新卡片')` 就變成 null，整個 `render()` 靜靜掛掉——熱圖會整張不見但沒有錯誤訊息。
這次加 `cardStuck` 時就中了一次。

**最貴的坑（全在 `PITFALLS.md`）：**

- **GAS 查詢參數不可叫 `c`**：保留字，回 400 但訊息像部署失敗。排查花最久
- **反應毫秒必須整數**：`performance.now()` 回小數會讓整份上傳被退回，畫面卻顯示完成
- **假伺服器不驗證＝端到端沒驗證**：mock 現在會比照資料契約檢查 POST 內容
- **衍生資料表不可當名單**：Dashboard 誤用快照當全班，讓「樣本不足標斜紋」的保護失效
- **靜態檔快取**：推新版後裝置繼續跑舊版
- **診斷與練習資料刻意分開（D4）**：只做過練習的學生在教師頁預設視角是全空白，
  已加說明與一鍵切換，否則看起來像資料掉了
- **刪資料一定要連快照一起重算**：只刪場次不重算，熱圖會繼續顯示已刪資料的顏色
- **時間一律存 UTC、顯示轉當地**：連「同一天」的分組也要用台灣時區判斷
- **不同時間長度的練習成績不可互相比**：3 分鐘當然比 1 分鐘答得多
- **門檻只在呈現層套用（D3／D20）**：`progress` 端點也只回原始毫秒，
  改成回傳算好的等級的話，老師一改門檻歷史資料就作廢

**工作方式的教訓：**

- 測試指令不可用 `| tail` 接 `&&`（管線離開碼是 tail 的，測試失敗也會 commit）
- 腳本改檔案時字串裡的換行跳脫字元容易被吃掉、變成真的換行而弄壞 JS 字串
  （`teacher.html` 改用 `var NL = String.fromCharCode(10)` 迴避）
- 一次改太多段時前面的切片會讓後面比對失敗，逐段套用較安全
- 改了作答規則要回頭檢查測試的假設（自動送出讓「打 99」的測試連鎖失效）
- 端到端的 `pg.on('dialog', ...)` 是全域的，會讓後面「按取消」的測試永遠測不到；
  改用具名函式並在需要處 `remove_listener`
- 表格加了欄位（例如勾選框）要回頭修測試的 `nth-child` 選擇器；
  三張報表已加 `tblA`／`tblB`／`tblC` 的 id，不再依賴位置
- **改成分頁後，端到端測試要自己切頁籤**：原本「全部同時可見」的假設一次壞掉 4 處，
  每個區塊的測試前都要先 `pg.click('#tabBar button:text-is("…")')`

**環境：**

- Apps Script 專案 `13tSg_CWU3i9ohKepVSEJjDPUrg5kYmmvW2Px7NlWrKxbqg8QuwnDotfN`
  （**必須由編輯器介面建立**，API 建的無法正常提供網頁應用程式）
- 部署一律 `python tools/deploy_gas.py`（先檢查核心同步，更新既有部署，網址不變）
- 沒有 `gh` CLI，但 Git Credential Manager 的權杖有 `repo` 權限，可直接呼叫 GitHub API
- 雙 repo：`beike-ai` 主庫與獨立的 `times-table-diagnostic`，兩邊都要推
- 工具：`setup_sheet` / `deploy_gas` / `loadtest` / `stamp_assets` / `rename_class` / `sync-core-to-gas`
