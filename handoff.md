# 工作交接 — 2026-08-29

## 已完成

**從零到上線，Phase 1 全部完成（25 個實作步驟）**

- 設計文件 v1.2（含 Codex 交叉審查 33 條，採納 29／部分採納 4）
- 四個頁面：入口、診斷作答、學生個人頁、教師 Dashboard
- GAS 後端 ＋ Google Sheets 四張表，含併發防護、去重、快照重算、資料保留政策
- GitHub repo 與 Pages 上線，後端部署至第 8 版

**上線後的三輪改進**

| 版本 | 內容 |
|---|---|
| v0.1.0 | Phase 1 首版 |
| v0.2.0 | 反應毫秒取整（修掉上傳被退回）、未完成場次也上傳、自選測驗範圍、送出鍵狀態 |
| v0.3.0 | 錯誤型態分析、教師頁單一學生檢視、家長通知訊息（複製／列印） |

**線上驗收（皆已通過）**

- 六個讀取端點正確，PIN 節流有效
- 33 筆併發上傳全成功、重送全判為重複、試算表剛好 33 列
- 真實瀏覽器對線上站台完整跑完 81 題，資料正確落入試算表
- 教師 Dashboard 以真實資料驗證，33 人 × 81 格算對
- 刪除本班資料的兩道防線有效

**測試：246 項核心 ＋ 66 項端到端，全過。** `python run-tests.py`

## 目前進度

**功能完整、線上可用，但還沒有任何真實學生用過。**

示範班可直接試：學生 `https://wukolo1206.github.io/times-table-diagnostic/?cls=DEMO01`，
教師 `teacher.html?cls=DEMO01`，PIN `1234`。

## 未完成／待確認

- **實機平板 ＋ 校內網路的 CORS 驗證**（唯一的硬性缺口，桌機測過不算數）
- 熟練門檻 3000 ms 未經本地實證，要用第一次真實診斷的分布校準
- 尚未建立任何真實班級（要班級名稱與人數才能開）
- Phase 2 精熟練習模式：設計已定案寫在 `DECISIONS.md`，尚未實作
- 使用者對「答錯扣分 vs 不加分＋鎖定」尚未拍板（DECISIONS Phase 2 的 P1）

## 下一步

1. **拿真平板到學校，連校內網路做一次完整作答**，確認畫面出現「成績已上傳」
   （選單一數字只要 9 題，兩分鐘）
2. 建立真實班級，跑一次全班課堂診斷
3. 用第一次的實際 ms 分布校準熟練門檻
4. 依 `DECISIONS.md` 的 Phase 2 章節做精熟練習模式

## 注意事項

**這次踩到的坑都寫在 `PITFALLS.md`，其中三個特別貴：**

- **查詢參數不可叫 `c`**：GAS 保留該名稱，帶 `c=` 的請求在進入 `doGet` 前就被回 400，
  錯誤訊息卻長得像部署失敗。排查花了最久。班級代碼一律用 `cls`
- **反應毫秒必須是整數**：`performance.now()` 回小數，會讓整份上傳被 `BAD_DETAIL` 退回，
  而且畫面顯示「完成」，學生和老師都不會知道資料掉了
- **假伺服器不驗證＝端到端沒驗證**：端到端的 GAS mock 現在會比照資料契約檢查 POST 內容，
  不再無條件回 ok

**工作方式上的兩個教訓：**

- 測試指令不可用 `| tail` 接 `&&`——管線的離開碼是 tail 的，測試失敗也會照樣 commit
- 用腳本改檔案時，`\n` 容易在傳遞過程被吃掉變成真換行，把 JS 字串弄壞。
  `teacher.html` 已改用 `var NL = String.fromCharCode(10)` 迴避

**環境：**

- Apps Script 專案 `13tSg_CWU3i9ohKepVSEJjDPUrg5kYmmvW2Px7NlWrKxbqg8QuwnDotfN`
  （**必須由編輯器介面建立**，用 API 建的那兩個已刪除）
- 部署一律 `python tools/deploy_gas.py`，會自動同步核心並更新既有部署，網址不變
- 這台機器沒有 `gh` CLI，但 Git Credential Manager 存的權杖有 `repo` 權限，
  可直接呼叫 GitHub API 建 repo／開 Pages
- 雙 repo：程式碼同時在 `beike-ai` 主庫與獨立的 `times-table-diagnostic`，兩邊都要推
