# 工作交接 — 2026-08-29

## 已完成

**從零到上線，並經真機實測與六輪改進（v0.1.0 → v0.6.0）**

| 版本 | 內容 |
|---|---|
| v0.1.0 | Phase 1：四頁 ＋ GAS 後端 ＋ 四張表，GitHub Pages 上線 |
| v0.2.0 | 修上傳被退回、未完成場次也上傳、自選測驗範圍 |
| v0.3.0 | 錯誤型態分析、教師頁單一學生檢視、家長通知訊息 |
| v0.4.0 | 精熟練習模式 `sprint.html`，與診斷分開 |
| v0.5.0 | 九枚徽章、個人最佳、還差幾格進度條 |
| v0.5.1 | 修 Dashboard 誤把快照當全班名單 |
| v0.6.0 | 一位數自動送出、個人頁加練習入口、結束畫面即時更新 |

**已建立真實班級：碧小408**

- 班級代碼 `49QY48`、教師 PIN `1257`、座號 1–32
- **純座號模式**：系統裡不存任何學生姓名
- 示範班 `DEMO01`（PIN `1234`、3 位假學生）保留供測試

**線上驗收（全部通過）**

- 33 筆併發上傳、去重、快照重算
- 真實瀏覽器完整跑完 81 題與精熟練習，資料正確落庫
- **使用者用手機 5G 實測整條路走通**（作答→上傳→錯誤分析→家長訊息）
- 教師頁刪除功能兩道防線有效

**測試：228 核心 ＋ 54 上傳層 ＋ 端到端全過。** `python run-tests.py`

## 目前進度

功能完整、線上可用、手機實測過。**碧小408 隨時可以進教室。**

## 未完成／待確認

- **真平板 ＋ 校內網路的驗證**（唯一硬性缺口；手機 5G 已通過，剩學校網路可能的防火牆／proxy）
- 熟練門檻 3000 ms 待用真實班級資料校準
- 精熟練習的「答錯懲罰」目前採建議方案（不扣分＋鎖 1.5 秒），
  使用者原本提過扣分，若要改是一行的事
- Phase 2 剩餘：三榜排行、全班同步衝刺、`admin.html`、
  設計文件 7.2 的遺忘因子（需快照存每格上次作答日期）

## 下一步

1. 拿真平板到學校連校內網路做一次完整作答，確認出現「成績已上傳」
   （選單一數字只要 9 題，或精熟練習 60 秒）
2. 碧小408 全班跑一次診斷，用實際反應時間分布校準熟練門檻
3. 依 `DECISIONS.md` Phase 2 章節做剩下的功能

## 注意事項

**改前端後一定要跑 `python tools/stamp_assets.py`**，否則裝置會繼續跑舊版且無任何錯誤訊息。

**本次踩到的坑（全部在 `PITFALLS.md`），最貴的五個：**

- **GAS 查詢參數不可叫 `c`**：保留字，回 400 但錯誤訊息長得像部署失敗。排查花最久
- **反應毫秒必須是整數**：`performance.now()` 回小數會讓整份上傳被退回，
  而畫面仍顯示「完成」，學生和老師都不會發現資料掉了
- **假伺服器不驗證＝端到端沒驗證**：mock 現在會比照資料契約檢查 POST 內容
- **衍生資料表不可當名單**：Dashboard 誤用快照當全班，導致「樣本不足要標斜紋」
  的保護失效，會拿一個人的資料建議明天教什麼
- **靜態檔快取**：推新版後裝置繼續跑舊版，症狀是「功能怎麼不見了」

**工作方式的教訓：**

- 測試指令不可用 `| tail` 接 `&&`（管線離開碼是 tail 的，測試失敗也會 commit）
- 用腳本改檔案時 `\n` 容易被吃掉變成真換行，把 JS 字串弄壞。
  `teacher.html` 已改用 `var NL = String.fromCharCode(10)` 迴避
- 一次改太多段時，前面的切片會讓後面的比對失敗；逐段套用比較安全

**環境：**

- Apps Script 專案 `13tSg_CWU3i9ohKepVSEJjDPUrg5kYmmvW2Px7NlWrKxbqg8QuwnDotfN`
  （**必須由編輯器介面建立**，API 建的無法正常提供網頁應用程式）
- 部署一律 `python tools/deploy_gas.py`（會先檢查核心同步，更新既有部署，網址不變）
- 這台機器沒有 `gh` CLI，但 Git Credential Manager 存的權杖有 `repo` 權限，
  可直接呼叫 GitHub API 建 repo／開 Pages
- 雙 repo：`beike-ai` 主庫與獨立的 `times-table-diagnostic`，兩邊都要推
