# DECISIONS — 九九乘法熟練度診斷

決策全文與理由見設計文件 `../docs/superpowers/specs/2026-08-28-times-table-diagnostic-design.md` v1.2 第 3 節。
此處為速查表。決策被推翻時用刪除線標記，不刪除。

| 編號 | 決策 | 一句話理由 |
|---|---|---|
| D1 | 線上作答自動收集，不做紙本輸入 | 紙本拿不到反應時間 |
| D2 | 「弱」＝對錯＋反應時間，四級判定 | 只看對錯抓不到「會但慢」 |
| D3 | 反應時間存原始毫秒，門檻只在呈現層套用 | 改門檻不得動底層資料，否則跨班跨時間不可比 |
| D4 | 標記模式與場合，診斷基準只採計「課堂×診斷」 | 回家環境不可控會失真 |
| D5 | 全測 81 格，不砍交換律 | 7×8 與 8×7 學生只背了一個方向 |
| D6 | 數字鍵盤填答，非選擇題 | 四選一 25% 猜中率，猜是「快且對」會被誤判為熟練 |
| D7 | 反應時間＝題目上屏→第一次數字鍵 | 測提取速度，不是手指靈活度 |
| D8 | 排行榜三榜並列（Phase 2） | 純速度榜會讓最需要練的人放棄 |
| D9 | 整場結束才上傳一次 | 逐題上傳＝2835 次請求／場 |
| D10 | 題目前端產生，作答全程離線可用 | 計時不受網路延遲影響 |
| D11 | `作答場次` 為唯一事實來源，快照為可重建衍生資料 | 快照算錯可完全重建 |
| D12 | 另存送出時間、修改次數、逾時與有效性旗標 | D7 可被「先亂按再想」規避 |
| D13 | 樣本不足不隱藏顏色，改標信心（n＋斜紋） | 首次診斷每格只有 1 筆，全灰則交付當天沒有價值 |
| D14 | 快照保存基準組與全量組兩份聚合 | 單一份無法同時支援 D4 的篩選 |
| D15 | POST 一律 `Content-Type: text/plain` | GAS 不回應 CORS preflight |
| D16 | 上傳延遲以座號取模決定，非隨機 | 隨機只是機率分散，不保證併發上限 |

## Q1（第一版範圍）——2026-08-28 使用者裁決

Phase 1 **只做診斷**，不含練習。閉環改由「系統前測 → 老師課堂補救 → 系統後測」達成。
已知代價：學生在兩次診斷之間沒有可操作的東西，回家情境 Phase 1 實質用不到。
若試教後老師反映「學生想練但沒得練」，即為啟動 Phase 2 的訊號。

---

## Phase 2 精熟練習模式（2026-08-29 與使用者討論定案，尚未實作）

**定位：分數是「練習過關標準」，不是診斷指標。**
熱圖負責判斷會不會，分數負責讓孩子想練。兩者分開，互不干擾。

（先前曾主張「分數會壓扁 81 格的資訊」而反對計分——那是針對**診斷**用途。
用在限時精熟練習上，計分是合適的，該反對意見不適用於此。）

### P1 限時＋計分

- 固定秒數（沿用班級設定的 `衝刺秒數`，預設 60 秒）
- 答對 +10 分
- **必須懲罰亂按**：限時模式下不罰的話，狂點鍵盤就是最佳策略
  （與診斷模式相反——診斷時罰錯會讓孩子不敢寫，反而掉資料）

| 做法 | 取捨 |
|---|---|
| 答錯扣 10 分 | 孩子直覺好懂，但**可能出現負分**，對後段學生打擊大。若採用須設下限 0 分 |
| **答錯不加分 ＋ 鎖定 1.5 秒**（建議） | 分數只往上不難看；限時模式裡時間比分數更痛，懲罰其實更重 |

### P2 過關標準必須因人而異

**不可訂全班統一分數門檻。** 那會讓快的孩子第一次就過關、之後沒動機，
慢的孩子練二十次都過不了、兩週後放棄——正好淘汰掉最需要練的人。

改為三顆星，門檻跟著自己走：

| | 條件 |
|---|---|
| ⭐ | 比自己上次進步 |
| ⭐⭐ | 達到自己設定的目標（沿用 `CPM目標`） |
| ⭐⭐⭐ | **該範圍所有格子都變綠** |

第三顆星直接扣回熱圖——**真正的「精熟」是熱圖全綠，不是分數到達某個數字。**

### P3 練習只出自己的弱格

抽題用設計文件 7.2 的分層抽樣（弱項 80%／熟練保底 20%），
不要讓已經很熟的 2×3 再佔用練習時間。保底那 20% 是為了避免全是不會的太挫折，
同時偵測熟練格是否退步。

### P4 施工順序

**先讓診斷版進教室跑一次再做練習模式。** 練習的抽題要靠熱圖判斷「哪些是弱格」，
熱圖若未經真實學生驗證就做練習，可能整批練錯方向。

### 資料結構已預留

不需要改任何資料格式：`作答場次` 已有 `CPM` 欄與 `設定.limitSec`，
快照的「全量組」本來就是給練習資料用的，`mode` 白名單已含 `sprint` / `practice`。

## D20：進步視圖比「第一次 vs 最近一次」，不比中位數

**2026-08-31**

多次練習後，原本的熱圖會失去價值——練過很多次還是紅的格子，
跟第一次就紅的格子長得一模一樣，老師分不出「還沒練」和「練不起來」。

**決定**：新增進步視圖，每一格拿每位學生的**第一次**與**最近一次**作答比較。

- 不用中位數：中位數會把早期的差表現一直拖著，練起來了也看不出來
- 不用「最近 N 次的平均」：那還是在描述現況，不是描述變化
- 一格只要**有一個人**練 3 次以上仍未熟練，整格就判紅——
  那一格靠學生自己練不會好，是老師該進場的地方，
  被「其他人都練起來了」稀釋掉就失去意義
- 只練過 1 次不算卡住（那只是還沒練）、只練過 2 次也不算（可能只是狀況差）

**門檻仍只在呈現層套用**：`progress` 端點只回原始毫秒，
等級與顏色由前端依當下門檻計算。老師把門檻由 3 秒改成 2 秒，
「誰算練起來了」會跟著重算，歷史資料不作廢（延續 D3）。
